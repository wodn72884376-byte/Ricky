---
omd: 0.1
brand: RICKY
bootstrapped_from: 29cm
bootstrapped_at: 2026-08-26
---

<!-- omd:unresolved: admin semantic color palette (success/warning/danger) — 레퍼런스는 marketing surface 전용이라 semantic 색 체계가 없다. 대시보드 경보는 §14에서 기존 토큰(반전 블랙 / #e8005d / #5d5d5d / opacity)만으로 표현한다. 새 색을 도입하려면 별도 결정이 필요하다. -->

<!--
omd:correction 2026-08-26 — 라이브 사이트 대조 후 세 항목을 정정했다.
이 문서는 레퍼런스 요약본에서 파생됐는데, 그 요약본이 라이브 29cm와 어긋나 있었다.
사용자가 실제 사이트 스크린샷을 대조해 잡아냈다.

  1. 컨테이너  1280px 중앙 정렬  →  폭 캡 없음(풀블리드 + 가터)
  2. 헤더      1행·로고 중앙     →  3행·작은 로고 좌상단
  3. 푸터      4열 균등          →  사업자 고지 블록(38%) + 링크 컬럼

  4. 타입 스케일  본문 계열 12/13/14/15  →  13/14/15/16, 상품명 15px
  5. 가터        48/32/20              →  24/20/16 (라이브는 사실상 0)
  6. 2행 메뉴    24-34px               →  28-40px (라이브 실측 40px/700)

교훈: 요약본과 라이브가 갈리면 **라이브가 이긴다**. 다음에 이 문서를 고칠 때도 같다.

라이브 실측 (2026-08-26, www.29cm.co.kr 카테고리 목록 + 상품 상세, 1920px):

    목록                          상세
    40px/700  편집 메뉴 2행        20px/700  상품명 · 할인율
    20px/700  섹션 헤딩            18px/700  브랜드명
    16px/600  카테고리 3행         16px/700  가격
    14px/700  가격 · 할인율        16px/400  상품 설명
    13px/400  상품명               14px/700  구매 CTA
    12px/700  브랜드명             14px/600  사이즈 표 헤더
    10-11px   배지 · 유틸리티      13px/500  할부 안내
    main 폭 1905 / 1920 (여백 ≈ 0)
-->

# Design System of RICKY

## 1. Visual Theme & Atmosphere

RICKY는 캘거리의 매장에서 직접 고른 물건을 한국으로 보내는 셀렉트 리테일이다 — 화면은 재고 목록이 아니라 인쇄된 룩북의 한 페이지처럼 읽혀야 한다. 기본 지면은 순백(`#ffffff`)이고 그 위에 순흑(`#000000`) 디스플레이 타입과 최소한의 크롬만 올라간다: 그림자 없음, 그라디언트 없음, 장식 아이콘 없음. 무게는 사진이 지고, 방향은 타이포그래피가 잡는다. 쿠팡이 효율이고 무신사가 스트리트 유틸리티라면 RICKY는 잡지다 — 넉넉한 여백, 크게 잡은 이미지 컬럼, 편집 블러브처럼 읽히는 캡션, 그리고 여백이 이 방에서 가장 비싼 재료라는 확신.

타이포그래피의 결정적 수는 **Pretendard Variable**을 전 surface에 걸쳐 세 단계의 좁은 위계로 운용하는 것이다: 30px/700 섹션 헤드라인, 22px/700 편집 카드 타이틀, 12-15px 본문. 디스플레이 전용 서체도, 장식 웨이트도 없다. 같은 한글 산세리프가 잡지 제목도, 상품 가격도, 푸터 주석도 처리한다. 버튼은 무장식이다 — 주력 CTA는 흰 배경에 1px `#C4C4C4` 아웃라인과 4px 코너를 가진 고스트 버튼(`더보기`)이고, 상시 노출되는 문의 어포던스(`FAQ`, `1:1 문의`)는 흰 글자를 얹은 `#000000` 사각형에 2px 반경으로, UI 컨트롤이 아니라 편집 캡션처럼 취급한다. 프라이머리 블루도, 액센트 퍼플도, 브랜드 레드도 없다. 브랜드 컬러는 색의 부재다.

두 번째 시그니처는 이미지 운용이다. 풀블리드 사진이 리듬을 만들고, 각 타일은 큰 이미지 하나와 그 아래 한국어 한 문장으로 구성된다. RICKY에서 그 사진의 절반은 브랜드 공식 컷이 아니라 **검수 사진** — 실물 택, 시리얼, 봉제, 사이즈 라벨을 찍은 것이다. 이 브랜드에서 사진은 분위기를 파는 도구이기 이전에 증거다. 가격은 강등된다: 12px/700로 잡아 12px/400 상품명 아래에 두고, 그 밑에 `관세·부가세 별도` 같은 수식 캡션을 muted `rgba(93,93,93,0.64)`로 흘린다 — 찾아보기 전에는 거의 보이지 않는 회색. 시각적 순서는 사진 먼저, 이야기 다음, 상품 그다음, 가격 마지막이다. 그 순서 자체가 브랜드의 명제다.

**Key Characteristics:**
- Pretendard Variable 단일 패밀리 — 세 웨이트(400 / 700 / 800), 디스플레이 컴패니언 없음
- 순수 `#ffffff` 지면, 순수 `#000000` 잉크 — 오프화이트도, 웜 뉴트럴도 없음
- 3단 디스플레이 위계: 30px 섹션 헤드라인 / 22px 카드 타이틀 / 12-15px 본문 및 가격
- Border-radius 절제: 반전 CTA 2px, 고스트 버튼 4px, 대부분의 편집 크롬은 0px
- muted 보조 텍스트는 `rgba(93,93,93,0.64)` — 스와치가 아니라 단일 투명도
- 고스트 버튼 외곽선은 1px solid `#c4c4c4` — 색을 넣지도, 두껍게 하지도 않음
- 12px/700 가격, 12px/400 상품명 — 둘 다 이미지에 종속
- 관세·부가세 예상액은 가격 하위 캡션 위계로 흡수 — 경고 배너가 아니라 주석
- 넉넉한 편집 패딩: 20-48px 지면 마진, 한글 본문 lh ≈ 1.36-1.5
- 그림자 시스템 없음 — 입면은 "들린 카드"가 아니라 "들린 사진"으로 읽힌다
- 대문자를 피한 영문 카테고리 라벨(`Arc'teryx`, `lululemon`, `Coach`, `New Arrivals`, `Sizing`)을 24-30px/700로 — 잡지 목차 레지스터를 그대로 옮긴 것

## 2. Color Palette & Roles

### Primary
- **Pure Black** (`#000000`): 본문 잉크, 헤딩, 반전 CTA 배경, 푸터 헤딩. 엄밀한 의미에서 이 브랜드의 유일한 "색".
- **Pure White** (`#ffffff`): 지면 배경, 고스트 버튼 배경, 반전 CTA 텍스트. 완전한 흰색 — `#fafafa`도 `#f5f5f5`도 아니다.

### Greys (functional, not branded)

회색은 **같은 색(`#5d5d5d`)의 두 단계**다. 나누는 기준은 밝기가 아니라 **정보를 싣느냐**이다.

- **Muted Text** (`#5d5d5d`, 불투명): `관세·부가세 별도`, `재고를 확인하고 있어요`, 예상 세액, 검수 사진 촬영 시각 — **읽혀야 하는 보조 텍스트**. 흰 배경 위 6.58:1로 WCAG AA를 통과한다.
- **Muted Decoration** (`rgba(93, 93, 93, 0.64)`): 비활성 내비게이션, 배지 카운트처럼 **놓쳐도 되는 것**. 흰 배경 위 2.92:1이므로 **정보를 실으면 안 된다.** 투명도이므로 뒤에 무엇이 오느냐에 따라 렌더 색이 달라진다.

> 이 둘을 혼동하는 것이 이 시스템에서 가장 하기 쉬운 접근성 위반이다. 판단 기준은 하나다 — **그 문장을 못 읽으면 구매 판단이 달라지는가.** 달라지면 Muted Text다.

보더도 두 단계다. 기준은 **경계가 그 컴포넌트를 식별하는 유일한 단서인가**이다.

- **Outline Grey** (`#c4c4c4`): 고스트 버튼처럼 **검은 텍스트 라벨이 함께 있는** 컨트롤. 라벨이 컴포넌트를 식별하므로 보더는 장식이고, 1.74:1이어도 무방하다.
- **Outline Strong** (`#949494`): 박스형 입력처럼 **보더가 없으면 컨트롤인지 알 수 없는** 곳. WCAG SC 1.4.11이 요구하는 3:1을 3.03:1로 충족한다.

### Accent / State (sparse)
- **Sale Red** (`#e8005d`): 할인 배지와 퍼센트 필에만 등장한다. UI 어포던스나 CTA에는 절대 쓰지 않는다. 인라인 폼 에러는 같은 계열의 `#ed0038`을 쓴다. 두 값 모두 흰 배경 위 4.5:1을 넘긴다 — 12px/700 배지와 13px/400 에러 텍스트는 '큰 텍스트' 예외에 해당하지 않기 때문이다.
- **Brand Black on Brand Black**: 상시 문의 CTA(`FAQ`, `1:1 문의`)와 단일 구매 CTA는 `#000000` 배경에 `#ffffff` 텍스트 — 지면의 반전이며, 고대비·저빈도 컨트롤로 취급한다.
- **운영 경보는 색이 아니라 반전과 위계로 표현한다.** 재고 미확인, 마진 미달 같은 대시보드 상태는 새 색을 도입하지 않고 반전 블랙 칩, `#e8005d` 텍스트, `#5d5d5d`, opacity 0.4의 네 가지 기존 수단으로만 구분한다(§14).

### What RICKY Explicitly Does Not Use
- 브랜드 블루, 브랜드 퍼플, 브랜드 그린 없음. `FAQ` / `1:1 문의` 버튼은 "프라이머리"가 아니라 푸터와 동일한 반전 블랙이다.
- 틴트된 뉴트럴 없음(`#fafafa` 없음, 웜 크림 없음). 배경은 `#ffffff`, 그걸로 끝.
- 그라디언트 없음. **벤토 스크림은 예외가 아니라 다른 범주다** — 색을 칠하는 것이 아니라
  검정 불투명도로 사진을 덮어 글자를 읽히게 하는 장치이며, 대비 요구치가 그 근거다(§4).
  브랜드 컬러 워시·그라디언트 텍스트·장식 그라디언트는 여전히 금지다.
- 스토어 surface에는 할인 레드 배지 외의 semantic 색 체계가 없다. success-green, warning-yellow, info-blue는 존재하지 않는다.
- 직구 커머스 관용의 원색 배지(형광 노랑 `무료배송`, 빨강 `관세폭탄주의`) 없음. 통관 정보는 경고색이 아니라 캡션 위계로 전달한다.

## 3. Typography Rules

### Font Family
- **Primary**: `Pretendard Variable`, 폴백 체인 `ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"`
- **단일 패밀리 시스템**: 별도 디스플레이 서체도, 세리프 컴패니언도, 모노스페이스도 없다. Pretendard Variable이 가독 surface의 100%를 담당한다. 단, 대시보드의 숫자 정렬에는 같은 패밀리의 `font-variant-numeric: tabular-nums`를 쓴다 — 서체를 바꾸지 않는다.

### Hierarchy

| Role | Size | Weight | Line Height | Notes |
|------|------|--------|-------------|-------|
| Section Headline | `clamp(30px, 2.6vw, 48px)` | 700 | ~1.13 | 뷰포트에 따라 자란다 |
| Sub-Headline | `clamp(24px, 1.9vw, 34px)` | 700 | ~1.21 | 카테고리 내비 · 벤토 소형 타일 |
| Editorial Card Title | `clamp(22px, 1.5vw, 28px)` | 700 | 1.36 | 시그니처 카드 타이틀 |
| Editorial Body / Description | 16px | 400 | 1.55 | 카드 설명, 편집 블러브, 검수 사진 캡션 |
| Nav Link (2행 대형) | `clamp(28px, 2.2vw, 40px)` | 700 / 800 | normal | 라이브 실측 40px/700. **비활성도 700이다** — 400은 얇아서 편집 메뉴로 안 읽힌다 |
| Nav Link (1행 유틸리티) | 13px | 400 | normal | 주문조회·장바구니·계정. 라이브는 10-13px |
| Nav Link (3행 브랜드) | 16px | 600 / 800 | normal | 라이브 실측 16px/600 |
| Floating CTA (`1:1 문의`) | 16px | 400 | normal | 검정 배경, 흰 텍스트, 2px 반경 |
| Ghost CTA (`더보기`) | 15px | 700 | normal | 흰 배경, 1px `#c4c4c4` 보더 |
| Footer Heading | 14px | 700 | 1.45 | 대문자 라틴 — `SHOP`, `GUIDE`, `HELP`, `ABOUT` |
| Footer Link / 고지 | 14px / 13px | 400 | 1.45 | 고지 문단은 `#5d5d5d` |
| Product Name (card) | 15px | 400 | 1.45 | 검은 잉크, 절제, 이미지 아래 |
| Product Price | 15px | 700 | 1.45 | **상품명과 같은 크기.** 라이브 29cm은 가격이 한 단계 크지만(14/700 vs 13/400) 우리는 §12-2 원칙을 지켜 같은 크기로 두고 굵기로만 구분한다 |
| Brand Name (card) | 13px | 400 | 1.4 | `#5d5d5d`. 상품명 위 |
| Price Caption (`관세·부가세 별도`) | 13px | 400 | 1.4 | `#5d5d5d` — Muted **Text** |
| Customs Estimate (예상 세액) | 13px | 400 | 1.4 | 가격 캡션과 동일 위계 |

> **디스플레이 사이즈는 고정 px가 아니라 `clamp()`다.** 지면에 폭 캡이 없으므로(§5)
> 고정값으로 두면 넓은 화면에서 헤드라인이 상대적으로 작아진다. 본문 계열은 고정이다 —
> 읽기 크기는 화면 폭이 아니라 사람의 눈에 맞춘다.
>
> **2026-08-26 정정:** 본문 계열을 한 단계씩 올렸다(12→13, 13→14, 14→15, 15→16).
> 12px 상품명은 좁은 카드를 전제한 값이라 풀블리드 그리드에서는 읽히지 않았다.

### Principles

- **한 패밀리, 세 웨이트.** Pretendard Variable을 400(본문·캡션·비활성 내비), 700(타이틀·가격·고스트 CTA), 800(활성 내비게이션)으로 쓴다. **200은 쓰지 않는다** — 16px 한글에서 획이 끊긴다. 이탤릭은 없다. 강조는 웨이트로만 하며 스타일로 하지 않는다.
- **편집 카드 타이틀 = 22-28px/700.** 이것이 목소리의 단위다. 대부분의 잡지형 타일은 22-29px 한국어 한 문장을 700 웨이트로 얹고, 같은 line-height 비율의 15px/400 설명을 뒤따르게 한다. 이 쌍이 브랜드의 문법이다.
- **라틴 라벨은 큰 사이즈에, 한글은 본문 사이즈에.** 카테고리 내비게이션은 영문(`Arc'teryx`, `lululemon`, `Coach`, `New Arrivals`, `Sizing`)을 디스플레이 사이즈(24-30px)로 쓰고, 한글이 본문과 편집 카피를 지배한다. 이 혼용은 의도된 것이다 — 영문은 잡지 목차의 레지스터로 기능한다. 브랜드명은 항상 공식 표기를 따른다(`lululemon` 소문자, `Arc'teryx` 아포스트로피).
- **가격은 카드에서 가장 큰 것이 될 수 없다.** 13px/700은 타이틀과 사진에 의도적으로 종속된다. 쿠팡이 가격을 맨 앞에 둔다면 RICKY는 맨 뒤에 둔다.
- **관세 정보는 가격의 각주다.** 예상 세액은 가격 캡션과 같은 12px/400 `#5d5d5d`에 놓는다. 크게 키우거나 색을 넣는 순간 페이지는 잡지에서 경고문으로 바뀐다. 다만 **각주도 읽을 수 있어야 각주다** — 이 텍스트에 2.92:1짜리 투명 회색을 쓰지 않는다.
- **자간은 거의 모든 곳에서 `normal`.** 타이트 트래킹도, 디스플레이 트래킹 보정도 없다. 이 서체는 개입 없이 디스플레이 사이즈를 감당할 만큼 잘 재단되어 있다.
- **행간 ≈ 1.36-1.50.** 한글 본문은 공기가 필요하고 편집 레지스터는 더 필요하다. 타이틀 1.36, 산문 1.50 — 그보다 좁히지 않는다.

## 4. Component Stylings

### Buttons

**Ghost Outline (주력 — `더보기`)**
- Background: `#ffffff`
- Text: `#000000`
- Border: `1px solid #c4c4c4`
- Radius: 4px
- Padding: 16px 16px 16px 20px (비대칭 — 후행 화살표 아이콘을 위해 왼쪽이 약간 넓음)
- Height: 52px
- Font: 14px Pretendard Variable weight 700
- Use: 모든 편집 섹션 말미의 `더보기 / 상품 더 보기`, 목록 더 불러오기의 기본 어포던스

**Inverted Solid (단일 고비용 컨트롤 — `장바구니 담기`, `FAQ`, `1:1 문의`)**
- Background: `#000000`
- Text: `#ffffff`
- Border: none
- Radius: 2px
- Padding: 4px 8px 4px 14px (비대칭 — 선행 아이콘 공간). 상품 상세의 구매 CTA는 16px 24px, height 52px로 확대 적용.
- Height: 31px (플로팅) / 52px (구매 CTA)
- Font: 15px Pretendard Variable weight 400 (플로팅) / 16px weight 700 (구매 CTA)
- Use: 우하단 고정 문의 컨트롤, 그리고 화면당 하나뿐인 구매 CTA. 한 화면에 두 개 이상 두지 않는다.

**Category Pill (내비게이션 카운트 배지 — `63`, `175`)**
- Background: transparent
- Text: `rgba(93, 93, 93, 0.64)` — 개수는 놓쳐도 되는 정보이므로 Muted Decoration을 쓴다
- Border: none
- Radius: 4px (컨테이너 상속)
- Padding: 0
- Height: 52px (탭 타깃, 시각 높이가 아님)
- Font: 16px Pretendard Variable weight 400
- Use: 브랜드/카테고리 내비 행의 개수 배지 — 숫자만, 큰 탭 타깃, 시각적 크롬 없음

**Sale Discount Pill** (상품 카드 오버레이)
- Background: `#ffffff` 또는 transparent
- Text: `#e8005d` (세일 레드)
- Border: none
- Radius: 0px
- Font: 12px weight 700
- Use: 상품 카드의 퍼센트 배지. 가격과 함께 놓이는 경우가 많다. `NEW`, `BEST`, `정품보장` 같은 라벨에는 쓰지 않는다 — 그것들은 웨이트만으로 표현한다.

### Cards & Containers

**Editorial Tile** (브랜드 스토리 / 큐레이션 / 검수 기록)
- Background: `#ffffff`
- Border: none
- Radius: 외곽 컨테이너 0px; 이미지도 0px(정사각 / 각진 모서리)
- Padding: 이미지는 풀블리드; 캡션 블록은 상단 16-20px 패딩
- Shadow: none — 분리는 사진과 여백이 만든다
- Title: 22px / 700 / `#000000` / lh 29.92px, 이미지 바로 아래
- Body: 15px / 400 / `#000000` / lh 22.5px
- Use: 홈의 지배적 단위 — 브랜드 스토리, 룩북, 큐레이션

**Product Card**
- Background: `#ffffff`
- Border: none
- Radius: 0px
- Image: 정사각 또는 4:5 세로, 0px 반경
- Title: 12px / 400 / `#000000`, 이미지 바로 아래
- Price: 12px / 700 / `#000000` (세일 시 `#e8005d`)
- Caption (`관세·부가세 별도`, `무료배송`, `재고를 확인하고 있어요`): 12px / 400 / `#5d5d5d` — **Muted Text**. 구매 판단에 쓰이는 문장이므로 투명 회색을 쓰지 않는다
- Spacing: 메타 블록의 행 사이 8-12px
- Use: 기본 카탈로그 아이템 — 이미지 우선, 텍스트 작게, 크롬 없음

**Bento Tile** (홈 브랜드 관문)
- Background: 사진 풀블리드. Radius 0px, 보더·그림자 없음
- **캡션을 사진 위에 얹는다** — §7의 "겹치지 않는다"에 대한 **유일한 예외**다.
  홈의 브랜드 벤토에만 적용하며 다른 곳으로 번지지 않는다.
- **스크림**: 하단에서 위로 `black/85 → black/70 → transparent`, 타일 높이의 60%.
  이것은 장식 그라디언트가 아니라 **흰 글자의 대비를 확보하는 기능 장치**다.
  그래서 불투명도에 하한이 있다 — 텍스트는 **70% 이상 구간 안에만** 놓는다.
  최악의 경우(순백 사진) 70% 스크림 위 흰 글자가 8.45:1로 AA를 통과하며,
  `npm run design:contrast`가 이 경우를 검사한다. 값을 낮추면 검사가 깨진다.
- Title: 라틴 브랜드명 30px/700(대형 타일) 또는 24px/700, `#ffffff`
- Body: 15px/400 `#ffffff`, 최대 46ch
- CTA: `on-image` 변형 — 투명 배경 + 1px `#ffffff` 보더 + 흰 글자, 4px 반경.
  호버 시 흰색으로 채워지고 글자가 검게 반전된다. 반전 블랙 CTA를 사진 위에 쓰지 않는다.
- **이 예외가 성립하는 조건**: 스크림이 있을 것, 대비 검사를 통과할 것, 홈 벤토일 것.
  셋 중 하나라도 없으면 캡션은 사진 아래로 내린다.

**Inspection Photo Card** (검수 기록)
- Background: `#ffffff`
- Border: none
- Radius: 0px
- Image: 실물 택 / 시리얼 / 라벨 / 봉제 클로즈업. 필터·보정 없음, 크롭만 허용.
- Caption: 15px / 400 / `#000000` 한 문장, 그 아래 촬영 시각을 12px / 400 / `#5d5d5d`
- Shadow: none
- Use: 상품 상세의 정품 증빙 갤러리, 주문 상세의 출고 전 검수 기록. 이 카드는 장식이 아니라 문서다 — 워터마크, 배지 오버레이, "정품 100%" 스탬프를 얹지 않는다.

### Inputs & Forms

- Background: `#ffffff`
- Border: 홈 검색 어포던스에는 없음; 포커스 시 하단 룰만
- Radius: 0px
- Font: Pretendard Variable
- Style: 편집형 — 검색은 박스형 UI 컨트롤이 아니라 헤더 바 입력으로 읽힌다. 상품 상세·체크아웃에서 박스형으로 쓸 때는 **1px `#949494`(Outline Strong)** 보더와 고스트 버튼에 맞춘 4px 반경을 쓴다. 입력은 보더가 사라지면 컨트롤인지 알 수 없으므로 버튼용 `#c4c4c4`를 쓰지 않는다.
- **개인통관고유부호 입력**: 박스형 규격을 그대로 따르되 `P` 접두를 포함한 13자를 한 필드로 받는다. 형식 오류는 인라인 에러(§14)로, 마스킹 표시는 `P1234****9012`.

### Navigation

- 헤더 배경: `#ffffff`. 하단에 `1px #c4c4c4` 헤어라인.
- **3행 구조다.** 한 줄에 다 넣지 않는다.

```
1행  h-14   RICKY(작게, 좌)                        주문조회 · 장바구니(n) · 계정 (우)
2행  h-16   BEST   Men's   Women's   이번 주 입고     ← 편집형 대형, 24px/700, 좌측 정렬
3행  h-11   Arc'teryx  lululemon  Coach │ Sizing  Customs   ← 조밀한 13px
```

- **로고는 작고 왼쪽에 있다.** 중앙 대형 워드마크가 아니다.
  1행은 유틸리티 행이고 브랜드 존재감은 2행의 편집형 메뉴가 만든다.
- 2행이 목소리다 — 디스플레이 사이즈의 라틴·한글 혼용 메뉴가 잡지 목차 역할을 한다.
- 3행은 브랜드를 상시 노출한다. 관문을 건너뛰는 경로이며 조밀한 소형 타이포로 둔다.
- 모바일에서는 1행만 남기고 2·3행은 햄버거 시트로 접는다.
- 스티키: 3행 전체가 붙어 있는다.

**메가 드롭다운 (`Men's` / `Women's`)**
- 헤더 전체 폭 패널, 안쪽은 같은 가터에 정렬. 3컬럼.
- 컬럼 헤더 = 브랜드 라틴 표기 24px/700 → 그 아래 `1px #c4c4c4` 룰 → 카테고리 목록 15px/400.
- **카테고리는 브랜드마다 다르다.** 코치에 `아우터`를, 아크테릭스에 `지갑`을 두지 않는다.
- 그림자 없음. 하단 `1px #c4c4c4` 보더로만 지면과 분리한다.
- 호버로 열되 120ms 유예를 둔다(항목 사이를 지날 때 깜빡이지 않게). 클릭·포커스로도 열리고
  Esc·바깥 클릭으로 닫힌다. 모바일에서는 시트 안 아코디언으로 같은 내용을 편다.
- 로고: RICKY 워드마크
- 스티키 동작: 헤더는 유지, 스크롤 후 보조 카테고리 행은 내려놓는다
- 모바일: 햄버거 토글, 동일 위계의 전체 화면 시트

**계정 영역 (헤더 우측)**
- 비로그인: `로그인` 텍스트 링크(16px/400) + `회원가입` 고스트 버튼(31px, `size-control-sm`)
  — 반전 블랙을 쓰지 않는다. 화면당 하나뿐인 반전 CTA는 구매에 쓴다(§4 Buttons).
- 로그인: 계정 아이콘 + 셰브런 → 드롭다운(`z-dropdown` 100).
  항목은 `내 정보` · `주문 내역` · `찜한 상품` · `1:1 문의` · `로그아웃`.
  드롭다운 상단에 로그인 이메일을 12px `#5d5d5d`로 밝힌다.
- 드롭다운은 보더 `1px #c4c4c4` + 0px 반경, 그림자 없음. 항목 높이 44px.
  바깥 클릭·Esc로 닫히고, `ArrowDown`으로 첫 항목에 포커스가 간다.
- **아이콘은 채우지 않는다.** 선으로만 그리고 획 두께는 셰브런과 같은 1.5다.
  마케팅 surface의 장식 아이콘 금지는 유효하다 — 이건 기능 컨트롤이다.

### Footer

- 배경: `#ffffff`. 상단에 `1px #c4c4c4` 헤어라인.
- 패딩: 상단 40px / 하단 64px (비대칭 — 하단이 넉넉)
- **좌우 2블록이다.** 좌측 38%가 사업자·정책 고지, 우측이 링크 컬럼 4개.
  크로스보더 커머스에서 고지는 부록이 아니라 신뢰의 일부다 — 작게 두되 **넓게** 둔다.
- 섹션 헤딩: 13px / weight 700 / `#000000`, 대문자 라틴(`SHOP`, `GUIDE`, `HELP`, `ABOUT`)
- 본문 링크: 13px / weight 400 / `#000000`. 탭 높이 44px
- 고지 문단: 12px / weight 400 / `#5d5d5d`. 법적 고지에 한해 `~합니다` 종결을 허용한다.
- **통신판매업 신고번호 항목을 두지 않는다** — 미신고 상태다 (docs/IA.md §5-7).

### Decorative Elements

- **히어로·편집 surface에 아이콘 없음.** 장식 그래픽은 의도적으로 부재한다 — 이미지가 곧 장식이다.
- **고스트 CTA의 후행 화살표.** `더보기` 버튼은 오른쪽에 얇은 `>` 셰브런을 단다 — 마케팅 surface에서 체계적으로 쓰는 유일한 아이콘이다.
- **세일 레드는 단 하나의 액센트.** 퍼센트 리본이 등장하면 그 타일에서 유일한 채색 순간이다. 절제가 그것을 작동하게 한다.
- **정품 보증을 배지로 만들지 않는다.** 신뢰는 방패 아이콘이 아니라 검수 사진과 영수증으로 증명한다.

## 5. Layout Principles

### Spacing System
- 기본 단위: 4px (4-8-12-16-20-24-32-48 캐스케이드)
- 지면 가로 마진: 데스크톱 24px, 태블릿 20px, 모바일 16px
- 편집 타일 간격: 주요 섹션 사이 24-32px
- 카드 메타 간격(이미지 → 타이틀 → 가격 → 관세 캡션): 행 사이 8-12px
- 푸터 블록 패딩: 상단 20px / 하단 48px — 비대칭, 하단이 넉넉

### Grid & Container
- **최대 폭 캡이 없다.** 지면은 화면 끝까지 가고 가터로만 여백을 만든다.
  사진이 무게를 지는 지면에서 폭을 1280px로 묶으면 큰 화면일수록 사진이 작아진다 —
  넓은 모니터에서 좌우가 텅 비는 것이 이 브랜드의 여백이 아니다.
- **읽기 위한 텍스트만 폭을 제한한다.** 산문 `--measure-prose: 640px`,
  좁은 폼 `--measure-form: 480px`. 그리드·사진·목록에는 적용하지 않는다.
- 편집 그리드: 태블릿 2열, 데스크톱 상품 타일 3-4열 / 스토리 1-2열
- 히어로: 보통 풀블리드 이미지에 30px/700 캡션이 아래에 놓인다 — 겹치지 않는다
- 카테고리 내비 행: 모바일 가로 스크롤, 데스크톱 전체 노출
- 대시보드: 스토어와 같은 풀블리드 지면과 4px 스페이싱을 쓴다. 사이드바를 두지 않고 상단 유틸리티 행으로 처리해 같은 지면감을 유지한다. 운영자는 넓은 화면에서 표를 보므로 폭을 묶지 않는 편이 유리하다.

### Whitespace Philosophy
- **여백이 곧 브랜드 자산이다.** RICKY의 큐레이션은 더한 것이 아니라 덜어낸 것으로 증명된다. 48px 지면 마진, 32px 섹션 간격, 구분선의 부재가 그 구조적 약속이다.
- **사진이 두 번째 목소리다.** 이미지 컬럼은 텍스트 컬럼보다 크게 잡고, 캡션은 이미지와 경쟁하지 않도록 짧고 잘 조판한다.
- **가격은 목적지가 아니다.** 상품 카드의 세로 리듬 — 이미지, 타이틀, 가격, 관세 각주 — 은 숫자보다 이야기를 먼저 읽게 한다.

### Border Radius Scale
- 0px: 이미지, 편집 타일, 검색 입력, 카테고리 라벨 — 대부분의 surface
- 2px: 반전 블랙 CTA(`장바구니 담기`, `FAQ`, `1:1 문의`) — 시스템에서 가장 작은 반경을 가장 고대비인 컨트롤에
- 4px: 고스트 아웃라인 버튼(`더보기`), 상품 상세·체크아웃의 박스형 입력
- 필 형태 없음, 마케팅 surface에 8px 초과 반경 없음


### Z-Index Scale

지면이 평평하다는 것은 *시각적* 진술이지 *스태킹* 진술이 아니다. 겹침은 존재하며, 순서는 고정한다.

| Token | Value | Use |
|---|---|---|
| `z-base` | 0 | 일반 콘텐츠 흐름 |
| `z-raised` | 10 | 스티키가 아닌 국소 겹침 — 상품 카드의 세일 필, 관문 캡션 |
| `z-dropdown` | 100 | 목록 필터 드롭다운, 정렬 메뉴 |
| `z-sticky` | 200 | 글로벌 헤더, 모바일 하단 구매·결제 바 |
| `z-floating` | 300 | 우하단 상시 문의 CTA |
| `z-scrim` | 400 | 모바일 시트·뷰어 배경 |
| `z-sheet` | 500 | 햄버거 전체화면 시트, 필터 하단 시트, 검수 사진 확대 뷰어 |
| `z-toast` | 600 | 하단 중앙 토스트 |
| `z-tooltip` | 700 | 최상위. 이 시스템에 툴팁은 거의 없지만 층은 비워둔다 |

- **플로팅 문의 CTA는 `z-floating`이며 `z-sheet`보다 낮다.** 시트가 열리면 문의 버튼은 가려진다 — 두 개의 어포던스가 동시에 떠 있으면 안 된다.
- 스크림은 `rgba(0,0,0,0.4)`가 아니라 **`#000000` opacity 0.4**로 적는다. 새 색을 만들지 않는다.
- 임의 z-index 리터럴(`z-index: 9999`)을 쓰지 않는다. 층이 부족하면 이 표를 고친다.

### Container & Grid Tokens

| Token | Value | Note |
|---|---|---|
| `gutter-mobile` | 16px | 라이브 29cm은 좌우 여백이 사실상 0이다. 최소한만 남긴다 |
| `gutter-tablet` | 20px | |
| `gutter-desktop` | 24px | |
| `measure-prose` | 640px | 산문 읽기 폭. 그리드에는 쓰지 않는다 |
| `measure-form` | 480px | 좁은 폼(주문조회·로그인) |
| `cols-product-mobile` | 2 | 상품 그리드 |
| `cols-product-tablet` | 3 | |
| `cols-product-desktop` | 4 | 1024px+ |
| `cols-product-xl` | 5 | 1440px+ |
| `cols-product-2xl` | 6 | 1536px+ — 폭 캡이 없으므로 열을 늘려 카드가 부풀지 않게 한다 |
| `cols-editorial-mobile` | 1 | 편집 타일 그리드 |
| `cols-editorial-tablet` | 2 | |
| `cols-editorial-desktop` | 2 | 밀도 높은 큐레이션 스트립에 한해 3열 허용 |

- **열 수는 명시적으로 쓴다.** `repeat(auto-fit, minmax(280px, 1fr))`를 쓰지 않는다 — 상품 카드는 이미지 비율이 고정이라 열 수가 흔들리면 리듬이 깨진다.
- 폼·산문 블록은 지면을 다 쓰지 않는다. 읽기 폭은 640px, 좁은 폼(주문 조회·로그인)은 480px다.

### Tap Targets & Control Heights

| Token | Value | Use |
|---|---|---|
| `size-tap-min` | 44px | 이 프로젝트가 정한 하한. WCAG 2.2 AA(SC 2.5.8)는 24px, 44px는 AAA(SC 2.5.5)·Apple HIG 기준이다 — **더 엄격한 쪽을 택했다** |
| `size-tap-nav` | 52px | 카테고리 배지·내비 링크의 탭 높이 (시각 높이와 무관) |
| `size-control-lg` | 52px | 고스트 CTA, 구매 CTA, 박스형 입력, 수량 스테퍼 |
| `size-control-sm` | 31px | 플로팅 문의 CTA의 **시각 높이**. 탭 영역은 패딩으로 44px를 확보한다 |
| `focus-ring-width` | 2px | §6 Focus |
| `focus-ring-offset` | 2px | §6 Focus |

- `size-control-sm`을 탭 타깃으로 착각하지 않는다. 31px는 그려지는 높이이고, 히트 영역은 상하 7px씩 확장한다.
- **문장 안 인라인 링크에는 44px을 적용하지 않는다.** WCAG 2.2 SC 2.5.8의 inline exception이다.
  억지로 키우면 줄 간격이 깨진다. 44px은 독립된 컨트롤(버튼·내비·목록 항목·카드)의 규칙이다.

### Aspect Ratios & Fixed Heights

| Token | Value | Use |
|---|---|---|
| `aspect-product` | 4 / 5 | 상품 카드 사진. 정사각(1/1)도 허용되나 한 목록 안에서 섞지 않는다 |
| `h-gateway-mobile` | 60vh | 홈 브랜드 관문 |
| `h-gateway-tablet` | 70vh | |
| `h-gateway-desktop` | 85vh | |

- 관문 사진은 모든 뷰포트에서 **종횡비를 유지한다.** 세로 크롭으로 대응하고 늘이지 않는다.


## 6. Depth & Elevation

| Level | Treatment | Use |
|-------|-----------|-----|
| Flat (Level 0) | 그림자 없음, 보더 없음 | 지면 배경, 편집 타일, 상품 카드 — 지배적 상태 |
| Outline (Level 1) | `1px solid #c4c4c4` | 고스트 CTA, 대시보드 테이블 행 구분 — 텍스트가 함께 있는 곳 |
| Outline Strong (Level 1b) | `1px solid #949494` | 박스형 입력 — 보더가 유일한 식별 단서인 곳 (WCAG 1.4.11) |
| Inverted (Level 2) | `#ffffff` 지면 위 `#000000` 배경 | 구매 CTA, 플로팅 문의, 푸터 헤딩 — "색 반전에 의한 입면" |
| Image-as-elevation | 흰 지면 위의 사진 | 편집 타일 — 사진 자체가 들린 레이어로 읽힌다, 그림자 불필요 |
| Focus | 2px 검정 아웃라인 오프셋 | 인터랙티브 요소의 키보드 포커스 링 |

**Shadow Philosophy.** RICKY의 마케팅 surface에는 그림자 시스템이 없다. 지면은 설계상 평평하며, 깊이는 CSS box-shadow가 아니라 흰 바탕 위의 사진이 만든다. "입면"에 가장 가까운 것은 고대비 반전 블랙 CTA이고, 이것은 UI 버튼이라기보다 편집 풀쿼트 콜아웃처럼 읽힌다. 타일에 `0px 4px 8px rgba(0,0,0,0.1)`을 넣는 순간 페이지는 "잡지"에서 "SaaS 대시보드"로 밀려난다 — **관리자 대시보드에서도 마찬가지다.** 대시보드의 카드도 그림자 없이 `1px solid #c4c4c4`와 여백으로만 구분한다.


### Elevation as Tokens (그림자 토큰이 없는 이유)

**`--shadow-*` 토큰은 이 시스템에 존재하지 않으며, 앞으로도 만들지 않는다.** 위 4단계 입면은
box-shadow의 강도 스케일이 아니라 **서로 다른 처리 방식**이므로, 하나의 스칼라 축으로 토큰화할 수 없다.
대신 각 레벨을 그 레벨이 실제로 쓰는 속성으로 정의한다.

| Level | Token | Value | 성격 |
|---|---|---|---|
| Flat (0) | `elevation-flat` | `none` | 값의 부재. 명시적으로 `none`을 두어 "아직 안 정한 것"과 구분한다 |
| Outline (1) | `elevation-outline` | `1px solid #c4c4c4` | `border` 속성 |
| Outline Strong (1b) | `elevation-outline-strong` | `1px solid #949494` | `border` 속성. 입력 전용 |
| Inverted (2) | — | `#000000` 배경 + `#ffffff` 텍스트 | **합성 레시피.** 단일 값이 아니므로 토큰이 아니라 컴포넌트 규격(§4)으로 산다 |
| Image-as-elevation | — | 흰 지면 위 사진 | 콘텐츠가 만드는 입면. 코드 토큰이 아니다 |
| Focus | `elevation-focus` | `2px solid #000000`, offset 2px | `outline` 속성 |

**구현 규칙.**
- `box-shadow`를 선언하는 커밋은 리뷰에서 거절한다 — 스토어든 대시보드든.
- 겹치는 요소를 분리해야 하면 순서대로 시도한다: (1) 여백을 늘린다 → (2) `elevation-outline` → (3) `Inverted`.
  세 가지로 안 되면 레이아웃이 잘못된 것이지 그림자가 필요한 것이 아니다.
- 예외는 **스크롤 콘텐츠와 겹치는 고정 바** 하나뿐이다(모바일 구매·결제 바). 여백이 작동하지 않는 유일한 경우이며,
  여기서도 그림자가 아니라 상단 `elevation-outline`을 쓴다.


## 7. Do's and Don'ts

### Do
- 모든 텍스트 요소에 Pretendard Variable을 쓴다 — 두 번째 서체는 없다.
- 모든 편집 타일에 사진 하나와 22px/700 한국어 짧은 캡션 하나를 짝지운다.
- `#000000`과 `#ffffff`만을 구조색으로 쓰고, 읽혀야 하는 보조 텍스트에 `#5d5d5d`를 쓴다.
- border-radius는 0px, 2px, 4px에 머문다 — 마케팅 surface에서 4px를 넘기지 않는다.
- 가격(12px/700)을 타이틀과 이미지에 종속시킨다. 이 순서를 절대 뒤집지 않는다.
- 관세·부가세 예상액은 반드시 결제 전에 보여주되, 가격 캡션 위계로 조용히 놓는다.
- 여백으로 분리한다 — 섹션 사이 24-48px, 보더나 구분선이 아니라.
- 반전 블랙 CTA는 화면당 한두 개의 고우선 어포던스에만 아껴 쓴다.
- 라틴 라벨(`Arc'teryx`, `lululemon`, `Coach`)을 디스플레이 사이즈로, 한국어 본문을 작은 사이즈로 섞는다.
- 정품 신뢰는 검수 사진과 구매 영수증·인보이스 이미지로 증명한다.

### Don't
- 편집 타일이나 상품 카드에 box-shadow를 넣지 않는다 — 지면은 평평하다.
- CTA에 브랜드 블루·레드·퍼플을 도입하지 않는다. 브랜드 "컬러"는 모노크롬이다.
- 필 형태 버튼이나 8px 초과 반경을 마케팅 surface 어디에도 쓰지 않는다.
- 가격을 카드 상단으로 올리거나 가장 큰 크기로 만들지 않는다 — 이미지 먼저, 가격 마지막.
- 강조에 이탤릭을 쓰지 않는다. 강조는 웨이트(400 → 700)로 한다.
- 배경을 틴트하지 않는다(`#fafafa`, `#f5f5f5`) — RICKY는 순백이다.
- 히어로 텍스트를 사진 위에 겹치지 않는다. 캡션은 이미지 *아래* 놓는다.
  **예외는 홈의 브랜드 벤토 하나뿐이며, 스크림과 대비 검사가 전제다** (§4 Bento Tile).
- 히어로에 장식 아이콘을 쓰지 않는다 — 이미지가 장식이다.
- **관세를 빨간 경고 배너로 만들지 않는다.** 사실은 각주로 전달한다. 겁을 주는 순간 이 브랜드는 직구 대행 사이트가 된다.
- **정품 보증을 방패·체크 아이콘 배지로 만들지 않는다.** 배지는 아무것도 증명하지 않는다.
- **재고를 모르는 상태에서 "구매 가능"으로 표시하지 않는다.** 확인되지 않았으면 그렇게 쓴다.
- **읽혀야 하는 문장에 `rgba(93,93,93,0.64)`를 쓰지 않는다.** 2.92:1이라 WCAG AA에 미달한다. 관세·재고·세액은 전부 `#5d5d5d`(6.58:1)다. 조용하게 두는 것과 안 보이게 두는 것은 다르다.

## 8. Responsive Behavior

### Breakpoints
| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <768px | 편집 타일 1열, 상품 그리드 2열, 햄버거 내비, 지면 마진 16-20px |
| Tablet | 768-1024px | 편집 2열, 상품 그리드 3열, 축약 내비 |
| Desktop | 1024-1440px | 편집 2-3열, 상품 그리드 4열, 전체 상단 내비 행 |
| Large Desktop | >1440px | 여전히 풀블리드. 상품 그리드 열 수만 늘린다 |

**Breakpoint tokens.** `--breakpoint-md: 768px` / `--breakpoint-lg: 1024px` / `--breakpoint-xl: 1440px` / `--breakpoint-2xl: 1536px`.
`2xl`은 **상품 그리드 열 증가에만** 쓴다. 프레임워크 기본값 중 `sm`(640px)은 **제거한다** — 이 시스템의 반응형 결정은 위 세 지점에서만
일어나며, 쓰지 않는 브레이크포인트가 남아 있으면 구현자가 그 지점에 규칙을 만든다.


### Touch Targets
- 카테고리 배지 버튼: 시각적으로 작아도 탭 높이 52px
- 플로팅 CTA(`FAQ`, `1:1 문의`): 31px 높이 — 모바일 최소 허용치, 8-12px 패딩과 함께
- 편집 타일: 타일 전체가 탭 가능, 별도 "보기" 버튼 없음
- 구매 CTA: 모바일에서 하단 고정 바로 전환, 높이 52px 유지

### Collapsing Strategy
- 섹션 헤드라인: 모바일에서 30px → 22px, 웨이트 700 유지
- 내비 행: 전체 가로 목록 → 햄버거 시트
- 상품 그리드: 4열 → 모바일 2열, 이미지 비율 유지
- 지면 마진: 24px → 모바일 16px
- 플로팅 문의 CTA: 모든 뷰포트에서 우하단 유지 — 유일하게 일관된 컨트롤
- 대시보드 테이블: 모바일에서 가로 스크롤 컨테이너로 감싼다. 열을 숨기지 않는다 — 운영자는 전체 숫자를 봐야 한다.

### Image Behavior
- 편집 사진: 모든 사이즈에서 풀블리드, 종횡비 유지
- 상품 카드: 정사각 또는 4:5 세로, 모바일에서도 유지
- 검수 사진: 원본 종횡비 유지, 확대 뷰어 제공. 압축으로 택·시리얼 판독이 어려워지면 안 된다.
- 카드처럼 스타일링된 레이지로드 스켈레톤 없음 — 플레이스홀더는 평평한 회색 블록, 시머 없음

## 9. Agent Prompt Guide

### Quick Token Reference
- 지면 배경: `#ffffff`
- 본문 텍스트: `#000000`
- muted 텍스트(정보 있음): `#5d5d5d` — 관세 캡션·재고 상태·촬영 시각. 6.58:1
- muted 장식(정보 없음): `rgba(93,93,93,0.64)` — 비활성 내비·카운트 배지. **문장에 쓰지 말 것**
- 아웃라인 그레이: `#c4c4c4` (텍스트 라벨 있는 버튼) / `#949494` (박스형 입력)
- 세일 레드: `#e8005d` (배지 전용) / 인라인 에러 `#ed0038`
- 프라이머리 CTA: 반전 블랙 — `#000000` 배경, `#ffffff` 텍스트, 2px 반경
- 세컨더리 CTA: 고스트 — `#ffffff` 배경, `#000000` 텍스트, `1px solid #c4c4c4`, 4px 반경
- 폰트 패밀리: `Pretendard Variable, ui-sans-serif, system-ui, sans-serif`
- 섹션 헤드라인: `clamp(30px, 2.6vw, 48px)` / 700 / lh 1.13
- 편집 타이틀: `clamp(22px, 1.5vw, 28px)` / 700 / lh 1.36
- 편집 본문: 16px / 400 / lh 1.55
- 상품명: 15px / 400
- 상품 가격: 15px / 700 (상품명과 같은 크기, 굵기로 구분)
- 관세 캡션: 13px / 400 / `#5d5d5d`


**Layout & Structure** (§5·§8)
- 컨테이너: **폭 캡 없음**. 가터로만 여백 (읽기 폭만 640/480으로 제한)
- 지면 마진: 모바일 `16px` / 태블릿 `20px` / 데스크톱 `24px`
- 상품 그리드: 2 / 3 / 4 / 5 / 6열 (모바일 → 초광폭. 명시적 열 수)
- 브레이크포인트: `768px` / `1024px` / `1440px` / `1536px` (마지막은 그리드 열 증가 전용)
- 탭 타깃 하한: `44px`. 내비·컨트롤 높이: `52px`
- z-index: base 0 / raised 10 / dropdown 100 / sticky 200 / floating 300 / scrim 400 / sheet 500 / toast 600 / tooltip 700
- 입면: `elevation-flat: none` / `elevation-outline: 1px solid #c4c4c4` / `elevation-focus: 2px solid #000000` offset 2px
- **그림자 토큰 없음.** `box-shadow` 선언 금지
- 상품 사진 비율: `4 / 5`

**globals.css `@theme` 추가분** — 위 토큰의 구현 형태. DESIGN.md가 원본이고 CSS는 파생이다.

```css
@theme {
  /* Layout — §5. 폭 캡 없음 — 지면은 화면 끝까지 간다. */
  --measure-prose: 640px;
  --measure-form: 480px;
  --gutter-mobile: 16px;
  --gutter-tablet: 20px;
  --gutter-desktop: 24px;

  --cols-product-mobile: 2;
  --cols-product-tablet: 3;
  --cols-product-desktop: 4;
  --cols-editorial-mobile: 1;
  --cols-editorial-tablet: 2;
  --cols-editorial-desktop: 2;

  /* Tap targets & control heights — §5.
     44px는 프로젝트가 정한 하한이다. AA(SC 2.5.8) 요구는 24px, 44px는 AAA(SC 2.5.5) 기준. */
  --size-tap-min: 44px;
  --size-tap-nav: 52px;
  --size-control-lg: 52px;
  --size-control-sm: 31px;    /* 시각 높이. 탭 영역은 패딩으로 44px 확보 */

  /* Aspect & fixed heights — §5 */
  --aspect-product: 4 / 5;
  --h-gateway-mobile: 60vh;
  --h-gateway-tablet: 70vh;
  --h-gateway-desktop: 85vh;

  /* Elevation — §6. box-shadow 토큰은 의도적으로 존재하지 않는다. */
  --elevation-flat: none;
  --elevation-outline: 1px solid var(--color-outline);
  --elevation-focus: 2px solid var(--color-ink);
  --focus-ring-width: 2px;
  --focus-ring-offset: 2px;

  /* Breakpoints — §8. 이 시스템의 브레이크포인트는 셋뿐이다. */
  --breakpoint-sm: initial;   /* 640px 제거 */
  --breakpoint-md: 768px;
  --breakpoint-lg: 1024px;
  --breakpoint-xl: 1440px;
  --breakpoint-2xl: 1536px;   /* 초광폭에서 상품 그리드 열을 늘리는 데만 쓴다 */
}

/* Z-index — §5. Tailwind에 z 네임스페이스 확장이 없어 커스텀 속성으로 둔다. */
:root {
  --z-base: 0;
  --z-raised: 10;
  --z-dropdown: 100;
  --z-sticky: 200;
  --z-floating: 300;
  --z-scrim: 400;
  --z-sheet: 500;
  --z-toast: 600;
  --z-tooltip: 700;

  /* 보조 모션 — §15 */
  --motion-stagger: 100ms;
  --motion-skeleton-pulse: 1000ms;
  --motion-toast-dwell: 3000ms;   /* dwell은 모션이 아니다 — 아래에서 붕괴시키지 않는다 */
  --scale-image-hover: 1.04;
}

@media (prefers-reduced-motion: reduce) {
  :root {
    --motion-stagger: 0ms;
    --motion-skeleton-pulse: 0ms;
    --scale-image-hover: 1;
    /* --motion-toast-dwell은 유지한다. 읽을 시간을 뺏지 않는다. */
  }
}
```


### Example Component Prompts
- "편집 타일을 만들어라: 상단 풀블리드 사진, 16px 간격, 22px Pretendard Variable weight 700 한국어 헤드라인 `#000000`, 8px 간격, 15px weight 400 설명. 보더 없음, 그림자 없음, 반경 없음. 흰 지면."
- "`더보기` 고스트 버튼을 만들어라: 흰 배경, 1px solid `#c4c4c4` 보더, 4px 반경, 14px Pretendard Variable weight 700 검정 텍스트, 패딩 16px 16px 16px 20px, 높이 52px. 오른쪽 끝에 후행 셰브런 `>`."
- "상품 카드를 만들어라: 정사각 사진 0px 반경, 12px Pretendard Variable weight 400 상품명 `#000000`, 바로 아래 12px weight 700 가격. 그 아래 캡션 행 `관세·부가세 별도`를 12px weight 400 `rgba(93,93,93,0.64)`로. 카드 보더 없음, 그림자 없음."
- "검수 사진 갤러리를 만들어라: 0px 반경 이미지 그리드, 각 이미지 아래 15px weight 400 한 문장 캡션과 12px weight 400 `rgba(93,93,93,0.64)` 촬영 시각. 워터마크 없음, 배지 오버레이 없음, 필터 없음."
- "관세 안내 블록을 만들어라: 흰 배경, 보더 없음, 15px weight 400 `#000000` 본문 두 문장. 예상 세액 숫자는 22px weight 700. 배경 틴트도, 경고 아이콘도, 빨간색도 쓰지 마라."
- "대시보드 상품 테이블을 만들어라: 흰 배경, 행 사이 `1px solid #c4c4c4`, 12px weight 400 셀 텍스트, 숫자 열은 `tabular-nums` weight 700 우측 정렬. 그림자 없음, 헤더 배경 틴트 없음, 헤더는 13px weight 700 대문자 라틴."
- "플로팅 문의 CTA: `#000000` 배경, `#ffffff` 텍스트, 2px 반경, 패딩 4px 8px 4px 14px, 높이 31px, 15px Pretendard Variable weight 400. 우하단 고정, 24px 인셋."

### Iteration Guide
1. 항상 Pretendard Variable을 쓴다 — 브랜드 서체다. 런타임이 로드하지 못하는 경우가 아니면 Noto Sans KR이나 Apple SD Gothic Neo로 대체하지 않는다.
2. 기본은 `#ffffff` 위 `#000000`이다. 보조 텍스트는 `#5d5d5d`, 순수 장식만 `rgba(93,93,93,0.64)`다. 그 외의 색은 구체적인 브랜드 이유를 댈 수 있을 때만. **바꾸기 전에 `npm run design:contrast`를 돌린다.**
3. 편집 카드 타이틀은 22px/700에 29.92px 행간이다. 이 쌍이 시스템에서 가장 특징적인 타이포그래피 단위이므로 정확히 복제한다.
4. 상품 가격은 12px/700이며 타이틀 아래에 산다. 위가 아니다. 다른 커머스 디자인을 이 시스템으로 옮길 때 가장 흔한 실수다.
5. 반경 스케일은 0 / 2 / 4다 — 8은 없고, 필도 없다. 4px 고스트 버튼이 마케팅 surface의 최대 반경이다.
6. 여백이 브랜드다. 레이아웃이 "비어 보인다"면 아마 정답에 가깝다. 채우고 싶은 충동을 견딘다.
7. 반전 블랙 CTA는 비싸다. 화면당 한두 번까지.
8. 사진이 시각적 무게를 진다. 이미지가 약하면 어떤 타이포그래피로도 이 레지스터를 복구할 수 없다 — 검수 사진의 품질이 곧 이 브랜드의 품질이다.
9. 관세·재고 정보를 디자인으로 강조하려 하지 않는다. 위계를 지키고, 대신 문장을 정확하게 쓴다.

---

## 10. Voice & Tone

RICKY는 세일을 돌리는 마케터가 아니라 물건을 골라 담는 편집자처럼 말한다 — 차분하고, 숙고하며, 들뜨지 않고, 급하지 않다. 한국어가 주 목소리이고 영어는 브랜드명과 섹션 라벨(`New Arrivals`, `Sizing`, `Customs`)에만 등장한다 — 인쇄 잡지의 관행에서 빌려온 것이지, 병렬 UI 언어로 채택한 것이 아니다. 소비자 대면 종결은 `~해요`이며 `~합니다`는 법적 고지 페이지에만 허용된다.

이 브랜드의 목소리에는 한 가지 특별한 부담이 있다. **불확실한 것을 다뤄야 한다** — 현지 재고는 변하고, 관세는 통관 시점에 확정되며, 항공 화물은 늦어질 수 있다. 이때의 원칙은 하나다: 모르는 것은 모른다고 쓰고, 아는 것은 정확한 숫자로 쓴다. 얼버무리지도, 겁주지도 않는다.

| Context | Tone |
|---|---|
| 편집 카드 타이틀 | 짧은 한국어 구절, 감각적이거나 담백하게 (`캘거리의 3월`, `한국에 없는 사이즈`). 명령형 없음. |
| 편집 카드 본문 | 감각적인 한 문장 + 초대. `현지 매장에서 직접 고른 이번 주 입고분을 같이 살펴봐요.` 종결은 `~해요`. |
| 상품명 | 브랜드 주도, 절제. `Arc'teryx Beta LT 자켓 블랙`. 브랜드 공식 표기를 따른다. |
| CTA | `더보기`, `자세히 보기`, `장바구니 담기`. `~기` 종결의 맨 동사 어간. 영어(`SHOP NOW`)도, 느낌표도 없다. |
| 가격 캡션 | 숫자 뒤에 `원`. 수식 캡션(`관세·부가세 별도`, `무료배송`)은 가격 아래 muted 회색으로 조용히. |
| 관세 안내 | 사실만. `미화 150달러 이하는 관세와 부가세가 면제돼요. 이 상품은 예상 세액이 약 68,000원이에요.` 추정치는 반드시 `예상`, `약`으로 표시한다. |
| 재고 미확인 | 상태를 그대로 쓴다. `재고를 확인하고 있어요` — `일시 품절`이나 `준비 중`으로 바꾸지 않는다. |
| 검수 사진 캡션 | 무엇을 찍었는지 한 문장. `실물 택과 시리얼 번호예요. 2026년 3월 4일 촬영.` 형용사를 붙이지 않는다. |
| 배송 안내 | 주기와 소요를 숫자로. `주 3회 출고해요. 캘거리 출고 후 영업일 기준 4~10일 걸려요.` |
| 교환·반품 | 조건을 먼저, 절차를 다음에. 사과로 시작하지 않는다. |
| 빈 상태 | 부재를 설명하는 한국어 한 줄 — `아직 찜한 상품이 없어요`. `데이터가 없습니다`는 쓰지 않는다. |
| 대시보드 경보 | 운영자용은 더 짧고 건조하게. `아크테릭스 수집 3회 연속 실패` — 감탄사도, 이모지도 없다. |

**Forbidden phrases.** `최저가`, `초특가`, `긴급세일`, `오늘만`, `마감 임박`, 영문 감탄형 CTA(`SHOP NOW!`, `BUY!`), 마케팅 surface의 모든 이모지, 카운트다운 타이머, 그리고 소비자 대면 문구에서의 `~합니다` 종결(법적 고지 전용). 한 상품에 겹쳐 쌓는 최상급(`프리미엄 럭셔리 시그니처 컬렉션`)은 직구 프로모 등급으로 읽히며 오프브랜드다.

이 브랜드에만 해당하는 금지어가 셋 더 있다:

- **`구매대행`, `대행`, `대행 수수료`** — 고객 대면 문구에서 금지한다. RICKY는 물건을 직접 매입해 자기 이름으로 파는 소매상이며, 대리인이 아니다. 이 표기는 브랜드 톤의 문제이기 이전에 사업 구조의 문제다(PROJECT.md §3.1).
- **`관세 폭탄`, `세금 폭탄`** — 통관 비용을 재난으로 프레이밍하지 않는다. 숫자로 예고하면 폭탄이 아니다.
- **`100% 정품 보장!!!`류의 강조** — 정품은 느낌표가 아니라 검수 사진과 영수증으로 증명한다. `모든 상품은 캐나다 공식 매장에서 매입해요`라고 사실을 쓴다.

## 11. Brand Narrative

RICKY는 캐나다 알버타주 캘거리를 거점으로, 아크테릭스·룰루레몬·코치를 비롯한 프리미엄 패션을 한국 고객에게 판매하는 셀렉트 리테일이다. [FILL IN: 설립/운영 시작 시점] <!-- omd:limitation 설립 시점은 사용자가 제공하지 않았다. 출시 전 실제 값으로 교체하고, 지어내지 말 것. -->

거점 선택 자체가 이 브랜드의 첫 번째 논거다. 캐나다의 소비세는 연방 GST 5%와 주별 PST로 이원화되어 있는데, 알버타주는 **PST가 존재하지 않는 극소수의 주 중 하나로 오직 GST 5%만 적용된다**. PST를 더해 12%(BC)·13%(온타리오)를 무는 타주 대비, 같은 물건을 살 때 7~8%p의 원가 우위를 매입 시점에 확보한다는 뜻이다. 캘거리 북부의 크로스아이언 밀스를 비롯한 현지 매장이 소싱 채널이고, 캘거리에서 밴쿠버를 거쳐 인천으로 향하는 항공 화물이 배송 경로다.

브랜드의 명제는 한 문장이다: **"한국에 없는 것을, 캘거리에서 직접 사서 보냅니다."** <!-- omd:limitation thesis는 사용자 요청에 따라 제안된 문구다. 확정 전 §7 대안 검토 필요. --> 여기서 중요한 것은 *직접 산다*는 부분이다. RICKY는 고객의 심부름을 대신 하고 수수료를 받는 대행업체가 아니라, 자기 자본으로 물건을 사서 자기 이름으로 되파는 소매상이다. 그래서 청구서에는 원가와 수수료가 분리되어 나타나지 않고 통합된 원화 가격 하나만 찍히며, 국제 배송 중 발생한 파손과 분실의 1차 재무 책임도 고객이 아니라 RICKY가 진다. 이 구조는 캐나다 국세청(CRA)이 사업자를 '단순 대리인'이 아닌 '소매상'으로 인정하기 위한 요건이기도 하다.

고객에게 하는 약속은 넷이다.

- **100% 정품.** 모든 상품은 캐나다 공식 매장에서 매입한다. 증명은 선언이 아니라 문서로 한다 — 매입 영수증과 인보이스를 함께 제공한다.
- **검수 사진.** 출고 전 실물 택, 시리얼, 사이즈 라벨, 봉제를 촬영해 남긴다. 보정하지 않고, 워터마크를 얹지 않는다.
- **주 3회 출고.** 캘거리 출고 후 한국 자택까지 영업일 기준 4~10일.
- **교환·반품 가능.** 크로스보더 리테일에서 흔히 회피되는 지점이며, RICKY는 이것을 회피하지 않는다.

RICKY가 거부하는 것: 할인 압박 머천다이징(상시 세일 배너 없음), 긴급성 연출(카운트다운 없음, `마감 임박` 없음), 통관 비용의 사후 통보(예상 세액은 결제 전에 계산해 보여준다), 재고를 확인하지 않은 채 주문부터 받는 관행(공식몰 재고가 최근에 확인되지 않았으면 결제를 열지 않는다), 그리고 장식적 크롬(그라디언트 없음, 그림자 없음, 흑백을 넘는 브랜드 컬러 없음). RICKY가 받아들이는 것: 상점 안의 긴 편집, 사진을 주 목소리로 삼는 것, 한국어를 주 언어로 하고 영어는 잡지 관행에서만 빌려오는 것, 그리고 **고객은 구매자이기 이전에 읽는 사람이라는 확신**.

## 12. Principles

1. **이미지가 곧 헤드라인이다.** 모든 편집 타일은 사진으로 시작한다. 한국어 캡션은 그 아래 놓이며, 위에 겹치지 않는다. 이미지가 타일을 지탱하지 못하면 그 타일은 준비되지 않은 것이다.
   *UI implication:* 모든 편집 유닛의 상단 60-70%를 사진에 할애한다. 캡션은 공동 헤드라인이 아니라 종속물이다.

2. **가격은 문장부호이지 동사가 아니다.** 상품 가격은 12px/700 — 상품명과 같은 크기다. 가격을 승격시키는 것은 편집 위계를 위반하고 페이지를 할인 커머스로 재프레이밍한다.
   *UI implication:* 카드에서 가격이 상품명 크기를 넘지 않는다. 할인 레드는 퍼센트 배지에만 아껴 쓰는 단일 액센트다.

3. **여백이 잉크보다 비싸다.** 큐레이션은 더한 것이 아니라 덜어낸 것으로 증명된다. 48px 지면 마진, 32px 섹션 간격, 구분선의 부재가 구조적 약속이다.
   *UI implication:* 기본값을 더 넓은 패딩과 더 적은 보더로 잡는다. 레이아웃이 "널찍하다"고 읽히면 정답에 가깝다.

4. **한 서체, 세 웨이트, 이탤릭 없음.** Pretendard Variable이 타이포그래피 시스템의 전부다. 강조는 웨이트(400 → 700 → 800)로 한다. 세리프 컴패니언이나 디스플레이 서체를 더하면 편집 레지스터가 희석된다.
   *UI implication:* 두 번째 패밀리를 들이는 기여를 거절한다. 볼드가 유일한 강조 프리미티브다.

5. **한국어가 주 목소리이고 영어는 섹션 라벨이다.** 본문, 캡션, 에러 상태, 온보딩은 한국어다. 영어는 브랜드명과 최상위 카테고리에 디스플레이 사이즈로 등장한다 — 병렬 UI 언어가 아니라 인쇄 잡지 관행에서 빌려온 장식이다.
   *UI implication:* 본문 문자열에 영어 마이크로카피를 피한다.

6. **절제가 브랜드다.** 유일한 "색"은 할인 레드 배지다. 유일한 "입면"은 색 반전이다. 유일한 아이코노그래피는 후행 셰브런이다. 추가되는 모든 시각 요소는 사진과 경쟁한다.
   *UI implication:* 그림자·보더·아이콘·색을 더하기 전에 구체적인 편집상 이유를 댄다. 없으면 뺀다.

7. **가격은 하나로만 말한다.** 고객이 보는 상품 금액은 마진이 포함된 최종 원화 가격 하나다. 원가와 수수료를 분리해 보여주지 않으며, 원가·마진율·환율은 어떤 고객 대면 화면에도 등장하지 않는다. 이것은 디자인 선택이 아니라 사업 구조의 요건이다.
   *UI implication:* 상품 카드, 장바구니, 주문 내역, 결제 영수증, 발송 이메일 어디에도 가격 분해 항목을 만들지 않는다. 배송비만 별도 라인으로 분리한다.

8. **모르는 것은 모른다고 쓴다.** 현지 재고가 최근에 확인되지 않았으면 "구매 가능"이라고 표시하지 않고 "재고를 확인하고 있어요"라고 쓴다. 관세는 통관 시점에 확정되므로 화면의 숫자는 언제나 `예상`이다.
   *UI implication:* 재고·세액·배송일에는 확정과 추정을 구분하는 표기를 반드시 둔다. 마지막 확인 시각을 감춘 채 상태만 보여주지 않는다.

9. **통관은 각주이지 경고가 아니다.** 관세·부가세는 결제 전에 반드시 계산해 보여주되, 빨간 배너나 경고 아이콘이 아니라 가격 캡션의 위계로 전달한다.
   *UI implication:* 세액 정보에 `#e8005d`, 경고 아이콘, 틴트 배경을 쓰지 않는다. 12px/400 `#5d5d5d`가 기본 위계다. 조용히 두되 읽을 수는 있어야 한다.

10. **고객은 읽는 사람이다.** 브랜드 스토리와 검수 기록은 긴 편집으로 취급한다. 사이트는 클릭 속도가 아니라 머무는 시간에 보상한다. 전환은 주목을 따라오고, 주목은 큐레이션을 따라온다.
    *UI implication:* 긴 페이지를 허용한다. 브랜드 스토리를 `더보기`로 게이팅된 카드로 압축하지 않는다.

## 13. Personas

*아래 페르소나는 사용자가 정의한 두 개의 고객 세그먼트에 기반한 가상의 아키타입이다. 실존 인물이 아니다.*

**가격을 아는 사람.** 한국 정발가를 정확히 알고 있다. 아크테릭스 베타 재킷이 한국 공식몰에서 얼마이고 캐나다에서 얼마인지, 그 차이 중 얼마가 관세이고 얼마가 유통 마진인지 계산할 수 있다. 그래서 "싸다"는 말에 반응하지 않고 숫자에 반응한다 — 상품가, 배송비, 예상 세액이 각각 얼마인지 결제 전에 보여주지 않는 사이트를 신뢰하지 않는다. 최저가 배너는 오히려 의심의 신호로 읽는다. 한 번 계산이 맞아떨어지면 다음 구매는 비교 없이 한다.

**한국에 없는 것을 찾는 사람.** 원하는 제품이나 사이즈가 한국에 들어오지 않는다는 사실을 이미 확인하고 온 사람이다. 국내 정발 라인업에서 빠진 컬러웨이, 한국 사이즈 스펙에 없는 치수, 시즌이 지나 국내에서 사라진 모델을 찾는다. 이 사람에게 가장 중요한 정보는 가격이 아니라 **그것이 진짜로 존재하고, 진짜로 그 사이즈이며, 진짜로 정품인가**이다. 검수 사진의 택과 사이즈 라벨을 확대해서 본다. 재고가 확인되지 않은 상태로 주문을 받았다가 취소되는 경험을 한 번 하면 돌아오지 않는다.

<!-- omd:limitation 사용자가 제공한 세그먼트는 두 개다. 레퍼런스는 4개의 페르소나를 두지만, 없는 세그먼트를 지어내지 않았다. 세그먼트가 추가되면 같은 형식으로 확장할 것. -->

## 14. States

| State | Treatment |
|---|---|
| **Empty (찜 / 장바구니)** | `#000000` 15px weight 400 한국어 한 줄로 부재를 설명(`아직 찜한 상품이 없어요`, `장바구니가 비어있어요`), 그 아래 고스트 아웃라인 CTA 하나(`상품 둘러보기`). 일러스트도, 마스코트도 없다. |
| **Empty (검색 / 필터)** | 14px `#5d5d5d` 캡션 한 줄(`검색 결과가 없어요`). 버튼 없음 — 사용자가 직접 필터를 조정한다. |
| **재고 확인 중 (신선도 만료)** | 상품 카드 캡션 행에 12px weight 400 `#5d5d5d`로 `재고를 확인하고 있어요`. 구매 CTA는 disabled 처리(opacity 0.4, 보더 유지). 이미지를 흐리거나 회색조로 만들지 않는다 — 상품은 여전히 존재한다. |
| **품절 (공급처 out_of_stock)** | 상품명과 가격을 `#5d5d5d`로 낮추고 캡션에 `지금은 재입고를 기다리는 중이에요`. 고스트 CTA `재입고 알림 받기`. 대각선 취소선이나 `SOLD OUT` 오버레이를 쓰지 않는다. |
| **관세 예상 (면세)** | 가격 아래 12px weight 400 muted 캡션 `관세·부가세 면제 예상`. 강조하지 않는다. |
| **관세 예상 (과세)** | 같은 위계로 `관세·부가세 약 68,000원 예상 (통관 시 수취인 납부)`. 빨간색·경고 아이콘·틴트 배경 금지. 상세 계산은 접힌 영역에 15px weight 400으로. |
| **Loading (첫 페인트)** | 최종 레이아웃 지오메트리에 평평한 `#f5f5f5` 블록. 편집 타일에는 시머 애니메이션을 넣지 않는다 — 사진과 경쟁한다. |
| **Loading (추가 상품)** | 목록 하단에 `#000000` 3점 로더. 오버레이도, 스켈레톤도 없다 — 기존 목록은 계속 보인다. |
| **Error (인라인 필드)** | 입력에 1px `#ed0038` 보더, 아래에 `#ed0038` 13px weight 400 에러 텍스트. 색만으로 전달하지 않는다 — 에러 문장이 항상 함께 온다. 한 문장(`개인통관고유부호는 P로 시작하는 13자리예요`). 친근-격식 `~요` 종결. |
| **Error (토스트)** | `#000000` 배경, `#ffffff` 14px weight 400 텍스트, 반전 CTA와 같은 2px 반경, 하단 중앙, 3초 자동 해제. 아이콘 없음. |
| **Error (화면 차단)** | 서버 장애 전용. 흰 화면, 중앙에 `#000000` 16px weight 700 한 줄, 아래 고스트 CTA(`다시 시도하기`). 일러스트 없음. |
| **Success (인라인)** | 해당 컨트롤에서 `#000000`/`#ffffff` 반전이 200ms 번쩍 — 토글 전환, 찜 활성화. 일상 UI에 초록 체크마크를 쓰지 않는다. |
| **Success (주문 완료)** | 전용 확인 화면. 22px/700 흑백 주문번호, 아래 15px/400 항목 요약, 고스트 CTA 하나(`주문 내역 보기`). 금액은 22px/700에 천단위 구분. 예상 세액과 개인통관고유부호 마스킹 표시를 15px/400로 함께 둔다. |
| **Skeleton** | 최종 치수의 `#f5f5f5` 블록, 0px 반경. 1.0s 페이드 펄스, 시머 금지. 가격에는 쓰지 않는다 — 해결 전까지 `--`로 렌더한다. |
| **Disabled** | 버튼 opacity 0.4. 보더 색은 바꾸지 않는다 — 재활성화 시 지오메트리가 흔들리지 않도록 고스트 버튼은 `#c4c4c4` 보더를 유지한다. |
| **Sale badge active** | 흰 타일 모서리에 `#e8005d` 텍스트 — 마케팅 surface의 유일한 채색 상태. 퍼센트 할인에만 쓰고 `NEW`, `BEST`, `정품보장`에는 쓰지 않는다(그것들은 웨이트만으로). |
| **대시보드 — 승인 대기** | 반전 블랙 칩(`#000000` 배경 / `#ffffff` 텍스트 / 2px 반경 / 12px weight 700)에 건수를 표기. 색이 아니라 반전으로 주의를 끈다. |
| **대시보드 — 수집 실패 경보** | 12px weight 700 `#e8005d` 텍스트 한 줄과 마지막 성공 시각을 `#5d5d5d`로 병기(`아크테릭스 3회 연속 실패 · 마지막 성공 6시간 전`). 배경 틴트도, 아이콘도 없다. |
| **대시보드 — 마진 미달** | 해당 행의 마진 수치만 `#e8005d` weight 700. 행 전체를 칠하지 않는다. |

## 15. Motion & Easing

**Durations** (원시 밀리초가 아니라 이름으로):

| Token | Value | Use |
|---|---|---|
| `motion-instant` | 0ms | 토글 전환, 찜 버튼 상태 변화 |
| `motion-quick` | 150ms | 고스트 버튼 호버, 포커스 링 페이드 |
| `motion-standard` | 250ms | 기본 — 이미지 호버 스케일, 시트 열림, 타일 등장 |
| `motion-editorial` | 400ms | 편집 전환 — 히어로 이미지 크로스페이드, 검수 사진 갤러리 이동 |
| `motion-page` | 350ms | 전체 화면 라우트 전환 |

**Supplementary tokens** (위 5종에서 파생되지 않는 값):

| Token | Value | Use |
|---|---|---|
| `motion-stagger` | 100ms | 타일 등장 스태거 간격. §15-2의 80–120ms 범위를 단일 값으로 고정한 것 |
| `motion-skeleton-pulse` | 1000ms | §14 Skeleton의 페이드 펄스 주기. 시머가 아니라 opacity 왕복 |
| `motion-toast-dwell` | 3000ms | §14 Error 토스트 자동 해제. **모션이 아니라 읽기 시간이므로 reduce-motion에서 붕괴시키지 않는다** |
| `scale-image-hover` | 1.04 | §15-1 이미지 호버 스케일. reduce-motion에서 `1` |

<!-- omd:unresolved: §14 Success(인라인)의 "200ms 번쩍"은 위 duration 세트(0/150/250/350/400)에
     존재하지 않는 값이다. 구현 전에 셋 중 하나를 골라야 한다 — (a) `motion-quick`(150ms)으로 스냅,
     (b) `motion-standard`(250ms)로 스냅, (c) `motion-flash: 200ms`를 6번째 토큰으로 추가.
     이 패치는 어느 것도 적용하지 않았다. 임의로 정하면 §15 토큰 세트의 폐쇄성이 깨진다. -->


**Easings:**

| Token | Curve | Use |
|---|---|---|
| `ease-out` | `cubic-bezier(0.2, 0.0, 0.0, 1.0)` | 등장 — 시트 진입, 타일 등장, 호버 안착 |
| `ease-in` | `cubic-bezier(0.4, 0.0, 1.0, 1.0)` | 퇴장 — 해제, 팝 |
| `ease-standard` | `cubic-bezier(0.4, 0.0, 0.2, 1.0)` | 양방향 — 아코디언 확장, 탭 콘텐츠 |
| `linear` | `linear` | 편집 갤러리의 이미지 스크러빙 — 지각적 중립성 유지 |

**Signature motions.**

1. **이미지 호버 스케일.** 편집 타일 호버 시 이미지가 `motion-standard`·`ease-out`으로 1.04배가 된다. 주변 타일 크롬은 움직이지 않는다. 이 모션은 인터페이스가 아니라 사진의 것이다 — 사진이 커서 쪽으로 기울어진다.
2. **스크롤 시 타일 등장.** 편집 타일은 opacity 0에서 12px 위로 이동하며 `motion-editorial`·`ease-out`으로 페이드인한다. 타일 간 스태거는 80-120ms로, 대시보드식 팝인이 아니라 페이지를 넘기는 리듬을 흉내 낸다.
3. **CTA 호버 (고스트 아웃라인).** `#c4c4c4` 보더가 `motion-quick`으로 `#000000`까지 어두워진다. 채움은 바뀌지 않는다. 변화는 선언되지 않고 감지된다 — 편집적 절제의 전형이다.
4. **CTA 호버 (반전 블랙).** 배경이 `motion-quick`으로 4% 밝아진다. 변형도, 그림자 추가도 없다. 이 브랜드는 깊이를 애니메이션하지 않는다.
5. **검수 사진 확대.** 썸네일에서 전체 뷰어로 갈 때 `motion-editorial`·`ease-out` 크로스페이드를 쓴다. 줌 바운스나 스프링을 쓰지 않는다 — 이 이미지는 증거 문서이며, 연출하면 신뢰를 깎는다.
6. **Reduce motion.** `prefers-reduced-motion: reduce`이면 모든 `motion-*` 토큰이 `motion-instant`로 붕괴한다. 이미지 호버 스케일은 비활성화된다. 이동은 크로스페이드로 대체된다. 사이트는 계속 사용 가능하고, 다만 덜 움직인다.

<!--
OmD v0.1 Sources — RICKY

Bootstrapped from: 29cm (omd:init, inspired mode, 2026-08-26)
§1-9, §14-15의 토큰 값은 29cm 레퍼런스에서 변경 없이 승계했다.
delta_set.axes는 비어 있다 — 색상·반경·밀도 시프트가 요청되지 않았다.

사용자 제공 사실 (§11, §13):
- 브랜드명 RICKY
- 약속: 100% 정품 보장 / 주 3회 출고 / 교환·반품 가능 / 검수 사진 / 정품 인증 영수증·인보이스
- 고객 세그먼트 2종: 한국 정발가 대비 가격을 아는 고객, 한국에서 구할 수 없는 제품·사이즈를 찾는 고객

프로젝트 문서에서 승계한 사실 (§11, §12):
- 알버타 GST 5% / PST 없음, 타주 대비 7~8%p 원가 우위 — 첨부된 사업 가이드 문서
- 캘거리 → 밴쿠버 → 인천 항공 화물, 영업일 4~10일 — 동일 문서
- 소매상(Principal) 지위, 통합 단일가 원칙, DDU 관세 정책, 재고 신선도 게이트 — PROJECT.md §2·§3·§6

미확정 (출시 전 교체 필요, 지어내지 말 것):
- 설립/운영 시작 시점 (§11)
- thesis 문구 "한국에 없는 것을, 캘거리에서 직접 사서 보냅니다" — 제안된 안이며 확정되지 않았다 (§11)

레퍼런스 대비 의도적 확장:
- §14에 재고 신선도·관세 예상·대시보드 경보 상태를 추가했다. 새 색을 도입하지 않고
  기존 토큰(반전 블랙 / #e8005d / #5d5d5d / opacity 0.4)만으로 표현한다.
- §10 금지어에 `구매대행`, `관세 폭탄`을 추가했다. 전자는 사업 구조상의 요건이다.
- admin semantic color palette는 미해결로 남겨두었다 (파일 상단 omd:unresolved 참조).

해석적 진술(예: "이미지가 곧 헤드라인이다", "여백이 잉크보다 비싸다")은 레퍼런스의
편집적 읽기를 승계한 것이며, RICKY의 공식 성명이 아니다.
-->
